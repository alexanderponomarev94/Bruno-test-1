info:
  name: GetRequest1
  type: http
  seq: 1

http:
  method: GET
  url: URL?list=1s
  params:
    - name: list
      value: 1s
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
