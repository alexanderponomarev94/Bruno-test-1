info:
  name: PostRequest1
  type: http
  seq: 3

http:
  method: POST
  url: URL
  body:
    type: json
    data: |-
      {
        "id": 12353
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
